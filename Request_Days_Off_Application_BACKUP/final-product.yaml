apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: final-product
  namespace: days-off
spec:
  rules:
  - host: days-off89.com
    http:
      paths:
      - path: /database
        pathType: Prefix
        backend:
          service:
            name: database-service
            port:
              number: 3306

